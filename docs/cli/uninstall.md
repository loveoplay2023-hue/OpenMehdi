---
summary: "CLI reference for `openmehdi uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `openmehdi uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
openmehdi uninstall
openmehdi uninstall --all --yes
openmehdi uninstall --dry-run
```
